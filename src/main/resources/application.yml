spring:
  mvc:
    async:
      request-timeout: 10000
  ai:
    mcp:
      server:
        enabled: true
        name: "my-mcp"
        version: "0.0.1"
        type: async
        sse-message-endpoint: /mcp/messages
        sse-endpoint: /mcp/events
